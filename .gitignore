node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Credenciales: nunca se suben. Solo viaja el ejemplo sin valores reales.
.env*
!.env.example

# Export original de AI Studio y su carpeta vacía: no son parte del código.
dieselpro-erp.zip
dieselpro-erp/

# Preferencias personales de Claude Code: no se comparten.
.claude/settings.local.json

# Capturas y logs de verificación con Playwright: artefactos locales de prueba.
.playwright-mcp/

# Compilación de Android: se regenera con "npx cap sync android".
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
